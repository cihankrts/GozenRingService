<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cihan.gozenringservis">

    <application
        android:allowBackup="true"
        android:icon="@drawable/gozenpicture"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gozenpicture"
        android:supportsRtl="true"
        android:theme="@style/Theme.GozenRingServis">
        <activity android:name=".RequestsDetailsActivity"></activity>
        <activity android:name=".DriverActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>